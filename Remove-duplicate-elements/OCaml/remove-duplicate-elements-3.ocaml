let uniq l =
  let rec tail_uniq a l =
    match l with
      | [] -> a
      | hd::tl -> tail_uniq (hd::a) (List.filter (fun x -> x  != hd) tl) in
  tail_uniq [] l

let() =
  let lst = [1;2;3;4;5;6;7;3;1;2;2;4;6;8;7;8;9] in
  List.iter (Printf.printf "%d\n") (uniq(lst));

